{
  "name": "core",
  "version": "1.0.0",
  "main": "src/app.ts",
  "scripts": {
    "start": "ts-node src/app.ts",
    "build": "tsc -p tsconfig.json",
    "test": "jest"
  },
  "dependencies": {
    "fastify": "^5.1.0"
  }
}
